<% include ../partials/header %>
<div class="container">

    <div class="row">
        <div class="container mx-auto">
            <h4> Winkelwagen </h4>
            <a> Hieronder staan de producten die zich momenteel in uw winkelmandje bevinden.</a>
            <br />
            <a> Druk op de knop 'Afrekenen' om te beginnen met te betalen</a>
			<div class="container">
                <div class="row text-center" style="display:flex; flex-wrap: wrap;">
                    <!-- VERANDER DEZE LIJN ALS JE DINGEN IN WINKELWAGEN KAN ZETTEN -->
                <% User.shoppingcart.forEach(function(product){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="img-thmbnail" src="/image/<%= product.image %>"></div>
                            <p><%= product.name %></p>
                            <p><strong>€ <%= product.price %></strong></p>
                            <p><a href="/<%=product.category %>/<%= product._id %>" class="btn btn-primary">Meer info</a></p>
                        </div>
                    </div>
                <% }); %>
                </div>
			</div>
            <h3>De totale prijs is ____</h3>
            <a href="/purchase" class="btn btn-primary">Nu Afrekenen</a>

        </div>
    </div>

</div>

<% include ../partials/footer %>